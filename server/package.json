{
	"name": "server",
	"module": "index.ts",
	"type": "module",
	"scripts": {
		"start": "bun run index.ts",
		"build": "cd ../client && bun run build && cd ../server && mkdir client && cp -r ../client/dist client"
	},
	"dependencies": {
		"cookie": "^0.6.0",
		"ioredis": "^5.4.1",
		"isbot": "^4.1.0",
		"sanitize-html": "^2.13.0"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"@types/sanitize-html": "^2.11.0",
		"typescript": "^5.5.3"
	}
}
